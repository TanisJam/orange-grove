# Tasks — comparador-price-comparison

- [x] T1 — Scaffold local `comparador/` Next.js TypeScript app without adding production source integrations yet. (covers R12, R13)
- [x] T2 — Create the query page with a free-text input, submit action, and empty-query validation. (covers R1, R2)
- [x] T3 — Add `/api/search` route that accepts a non-empty query and returns normalized search state. (covers R2, R9, R12)
- [x] T4 — Define shared `PriceResult`, `SourceStatus`, and `SourceAdapter` types. (covers R2, R3, R5, R6, R9, R10, R11)
- [x] T5 — Implement the search service that runs only configured permitted adapters and aggregates results plus source statuses. (covers R2, R9, R10, R11)
- [x] T6 — Add a configurable source registry where unsupported, blocked, or unauthorized sources are marked unavailable instead of bypassed. (covers R10, R11)
- [x] T7 — Implement at least one permitted-source adapter only after confirming its allowed access method. (covers R2, R3, R5, R6, R10, R11)
- [x] T8 — Normalize source responses into result title, numeric ARS price, source name/type, listing URL, and retrieved timestamp. (covers R3, R5, R6)
- [x] T9 — Format all displayed prices with Argentina-localized ARS currency formatting. (covers R4)
- [x] T10 — Render comparable results in a table or card list that makes cross-source price differences clear. (covers R3, R5, R7)
- [x] T11 — Add listing links that open the original source listing without creating a purchasing flow. (covers R3, R13)
- [x] T12 — Show freshness context for every displayed result. (covers R6)
- [x] T13 — Show a clear no-results empty state for successful searches with zero matches. (covers R8)
- [x] T14 — Show source-level unavailable, rate-limit, and error messages without presenting incomplete data as authoritative. (covers R9, R10, R11)
- [x] T15 — Add request-level error handling for network or adapter failures that prevent reliable search completion. (covers R9)
- [x] T16 — Add focused tests or verification checks for query validation, ARS formatting, result normalization, empty state, and blocked-source behavior. (covers R1, R4, R8, R9, R10, R11)
- [x] T17 — Verify MVP boundaries: no accounts, payments, price history, alerts, browser extensions, or purchasing flows. (covers R12, R13)
