# Tasks — comparador-mercadolibre-source

- [ ] T1 — Add `comparador/src/sources/mercadolibre.ts` with a Mercado Libre Argentina `SourceAdapter` using `accessMethod: "official_api"`. (covers R1, R2, R3)
- [ ] T2 — Read `MELI_ACCESS_TOKEN` only from server-side runtime configuration and return `unavailable` with zero results when absent or blank. (covers R1, R4, R11)
- [ ] T3 — Build the search URL with `URLSearchParams` for `https://api.mercadolibre.com/sites/MLA/search`, including encoded `q`, `limit=10`, and `sort=price_asc`. (covers R2, R9)
- [ ] T4 — Send the Mercado Libre request with `Authorization: Bearer <token>` without logging or exposing the token. (covers R3, R4)
- [ ] T5 — Map valid Mercado Libre items from `id`, `title`, `price`, `currency_id`, and `permalink` into the existing raw result shape with `sourceName`, `sourceType`, `listingUrl`, and one `retrievedAt` timestamp per search. (covers R5, R7, R8)
- [ ] T6 — Exclude non-`ARS` listings instead of converting currencies implicitly. (covers R6)
- [ ] T7 — Exclude listings missing usable title, numeric price, or permalink before they reach the normalizer. (covers R14)
- [ ] T8 — Return `rate_limited` status for HTTP `429` without aggressive retries. (covers R10)
- [ ] T9 — Return `unavailable` status for HTTP `401` and `403` authorization/access failures, with no fake Mercado Libre fallback. (covers R11)
- [ ] T10 — Return source-level `error` status for network failures, timeouts, malformed JSON/shape, and other non-2xx responses while preserving other sources through the search service. (covers R12)
- [ ] T11 — Return `ok` with zero results and a no-results-aware message when Mercado Libre responds successfully with no usable listings. (covers R13, R14)
- [ ] T12 — Replace the Mercado Libre unavailable placeholder in `comparador/src/sources/index.ts` with the real Mercado Libre adapter while leaving demo/local data clearly separate. (covers R1, R3, R13)
- [ ] T13 — Add Mercado Libre adapter tests using mocked `fetch` for success mapping, URL/query params, token absence, `401`, `403`, `429`, generic failure, network failure, malformed response, no-results, ARS guard, incomplete listings, and no fake fallback. (covers R3, R4, R5, R6, R9, R10, R11, R12, R13, R14, R15)
- [ ] T14 — Add or update registry/search-service tests proving Mercado Libre status is aggregated without breaking valid results from other configured sources. (covers R1, R12, R15)
- [ ] T15 — Run only targeted tests/type checks available for `comparador/`; do not run a production build. (covers R15)
