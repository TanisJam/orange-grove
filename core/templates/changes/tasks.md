# Branches delta — <change-id>

> Owner: `branch-pruner`. Tasks scoped to this change only. Every task references either an ADDED, MODIFIED, or REMOVED requirement from `requirements.md` in this folder.

- [ ] T1 — <action> (covers R<n>)
- [ ] T2 — <action> (covers R<n>)
- [ ] T3 — Update test for R<n> behavior change (covers MODIFY R<n>)
- [ ] T4 — Delete test for R<n> (covers REMOVE R<n>)

> Tasks for a change apply on top of the base implementation. They do NOT re-implement the base.
