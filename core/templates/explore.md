# Soil — <feature>

> Owner: `soil-reader`. Written before any requirements. Describes what IS, not what SHOULD BE.

## Codebase context

- `path/to/file.ts:42` — what it does and why it matters here
- `path/to/module/` — relevant module

## Constraints discovered

- Tests run with `<command>`
- Lint / formatting requirements: `<tool>`
- Existing conventions to respect (naming, structure, patterns)
- Dependencies already present that the feature should reuse

## Related prior work

- Feature `<feature-id>` — what was done and why it matters here
- Commit `<sha>` — relevant decision

## Open questions

- Q1: <question that requirements must answer>
- Q2: <question that requirements must answer>
