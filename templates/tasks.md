# Branches — <feature>

> Owner: `branch-pruner`. Every task references at least one `Rn`. Every `Rn` is covered by at least one task.

- [ ] T1 — <small executable step> (covers R1)
- [ ] T2 — <small executable step> (covers R2, R3)
- [ ] T3 — Add unit test for <behavior> (covers R1)
