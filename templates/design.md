# Trunk — <feature>

> Owner: `trunk-shaper`. Reads Soil + Roots. The Test Strategy section is enforced by Ripening.

## Decision

<technical decision and why, referencing R1, R2, ...>

## Alternative considered

<what was rejected and the tradeoff that ruled it out>

## Test strategy

- R1 → unit test in `<file or module>`
- R3, R4 → integration test scenario `<name>`
- R5 → manual verification (justify why automated test is not appropriate)
