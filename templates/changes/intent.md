# Change intent — <change-id>

> Owner: `soil-reader` or the human seeding the change. The intent block scopes what this change does to its base feature. Lives in `specs/active/<base-feature>/changes/<change-id>/intent.md`.

## Targets

Base feature: `<base-feature-id>`

## Why this change

<one paragraph: what triggered this change, why now, what problem it solves>

## What it changes

- Adds: <Rn numbers that will be added>
- Modifies: <Rn numbers that will be modified>
- Removes: <Rn numbers that will be removed>

## What it does NOT change

<explicit out-of-scope so the implementer cannot drift>
